package Oct.Task_30102024;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

public class Ex_LinkedList {
    public static void main(String[] args) {
        List<Integer> mylist = new LinkedList<>();
        mylist.add(1);
        mylist.add(2);
        mylist.add(3);
        mylist.add(4);
        mylist.add(4);

        System.out.println(mylist);

        /*
         * Linked list allows duplicates and internally implements a doubly-Linked list data structure.
         * Linked list elements will be stored internally using node representation.
         * Fast to insert and delete but slow in accessing the elements
         */

        Integer[] a = new Integer[5];
        String [] str = new String[5];

        List<String> list = new ArrayList<>();
        list.add("Digambar");
        list.add("Anvi");
        list.add("Anita");

        //Selenium -API ->Store and Insertion and Deletion - DS - LL
        //API- Store and access-AL


    }
}